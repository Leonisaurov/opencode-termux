diff --git a/build.zig b/build.zig
index 1b3eae9..f3dbbc5 100644
--- a/build.zig
+++ b/build.zig
@@ -862,7 +862,9 @@ fn configureObj(b: *Build, opts: *BunBuildOptions, obj: *Compile) void {
     obj.use_llvm = !opts.no_llvm;
     obj.use_lld = if (opts.os == .mac or opts.os == .linux) false else !opts.no_llvm;
     if (opts.lto) {
         obj.lto = .full;
         obj.use_lld = true;
     }
 
-    obj.no_link_obj = opts.os != .windows and !opts.no_llvm;
+    if (@hasField(@TypeOf(obj.*), "no_link_obj")) {
+        obj.no_link_obj = opts.os != .windows and !opts.no_llvm;
+    }
 
     if (@hasField(Build.Module, "sanitize_address")) {
