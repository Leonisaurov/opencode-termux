diff --git a/codex-rs/tui/src/app/thread_routing.rs b/codex-rs/tui/src/app/thread_routing.rs
index 918bb091f0..1b50c0be1a 100644
--- a/codex-rs/tui/src/app/thread_routing.rs
+++ b/codex-rs/tui/src/app/thread_routing.rs
@@ -646,6 +646,11 @@ impl App {
                 });
                 Ok(true)
             }
+            AppCommand::Steer { items, turn_id } => app_server
+                .turn_steer(thread_id, turn_id.clone(), items.to_vec())
+                .await
+                .map(|_| true)
+                .map_err(|error| color_eyre::eyre::eyre!(error)),
             AppCommand::UserTurn {
                 items,
                 cwd,
