diff --git a/codex-rs/.cargo/config.toml b/codex-rs/.cargo/config.toml
index 8a6f2c11ee..43e2aeee23 100644
--- a/codex-rs/.cargo/config.toml
+++ b/codex-rs/.cargo/config.toml
@@ -9,3 +9,16 @@ rustflags = ["-C", "link-arg=/STACK:8388608", "-C", "link-arg=/arm64hazardfree"]
 
 [target.'cfg(all(windows, target_env = "gnu"))']
 rustflags = ["-C", "link-arg=-Wl,--stack,8388608"]
+
+[target.aarch64-linux-android]
+linker = "aarch64-linux-android-clang"
+ar = "llvm-ar"
+rustflags = [
+    "-C", "target-feature=-crt-static",
+    "-C", "link-self-contained=no",
+    "-C", "split-debuginfo=off",
+    "-C", "debuginfo=line-tables-only",
+    "-C", "force-frame-pointers=yes",
+    "-C", "link-args=-Wl,--build-id=none",
+    "-C", "link-args=-fuse-ld=lld",
+]
